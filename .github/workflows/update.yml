name: Daily ETF Update

on:
  schedule:
    - cron: '30 10 * * 1-5' # 10:30 UTC = 台灣時間 18:30
  repository_dispatch:
    types: [trigger-update] # 開放外部 API 觸發點
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Scraper Engine
        run: python update_data.py

      - name: Commit and push changes
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Action"
          git add data_*.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update dynamic data" && git push)

      - name: Telegram Status Notification
        if: always()
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          JOB_STATUS: ${{ job.status }}
        run: |
          if [ "$JOB_STATUS" == "success" ]; then
            MSG="✅ 台股全市場 ETF 數據庫與新聞動態更新成功！%0A執行時間：$(date +'%Y-%m-%d %H:%M:%S')"
          else
            MSG="❌ 台股 ETF 數據更新發生非預期失敗，請即刻前往 GitHub Actions 查看錯誤日誌。"
          fi
          curl -s -X POST "https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TG_CHAT_ID}" \
            -d text="$MSG"
